# Authenticating requests

To authenticate requests, include an **`Authorization`** header with the value **`"Bearer {ACCESS_TOKEN}"`**.

All authenticated endpoints are marked with a `requires authentication` badge in the documentation below.

Para probar los endpoints protegidos, utiliza las siguientes credenciales por defecto:

**Email**: `admin@admin.pe`
**Password**: `SecureAdmin2026!@#`

El token se genera automáticamente al hacer login con estas credenciales.
